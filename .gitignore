# OS
.DS_Store
Thumbs.db

# Editors
.vscode/*
!.vscode/extensions.json

# Env / secrets
.env
.env.local

# Node (로컬 정적 서버용 도구를 쓸 경우에만 생성됨)
node_modules/

# Figma 원본 시안 (참고 자료, 저장소에 커밋하지 않음)
1차 시안.zip

# TourAPI 배치 수집 스크립트가 생성하는 SQL (재생성 가능한 산출물, 검토 후 수동 실행)
supabase/generated/
